// ここから「ギャラリーとして動く」状態になる。
// 6. ItemList（仮データでOK）
// フィルタリング結果の一覧。
// - ギャラリーの中心
// - 仮データで動かせるので早期に動作確認できる
// - Lazy Loading やページネーションの土台になる

export function ItemList() {
  return (
    <div className="h-64 bg-neutral-100 rounded-md flex items-center justify-center text-neutral-400">
      ItemList（後で実装）
    </div>
  );
}
